package util;


import java.io.IOException;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
public class Main {
	public static void main(String[] args) throws IOException
	{
//		String filepath = "E:\\BIT\\FlakyTest\\FIles\\8.java";
//		Tools.Run(filepath, obj);
//		String pattern = "\\w*\\s*\\w+\\s*=\\s*\\w+\\.\\w+\\(";
//		ArrayList<String> filepaths = Tools.getallfilepaths();
//		
//		for (int i = 0; i < filepaths.size(); i++) {
////			int startpos = 0;
////			ArrayList<String> fileContent = FileUtil.readFile(filepaths.get(i));
////			int size = fileContent.size();
//			String initialpath = filepaths.get(i).split("\\.")[0];
//			System.out.println(initialpath.length());
//			String path = "";
//			for(int k=0;k<initialpath.split("\\\\").length-1;k++)
//				path= path + initialpath.split("\\\\")[k] + "\\";
////			String mainclass = Tools.findMainClassName(filepaths.get(i));
//			System.out.println(path);
//			// 对每个java文件定位到有函数调用且有返回值接受的那些代码行，拿到该行中接受返回值的对象，调用API的对象，行数以及调用的函数名称
//			ArrayList<Map<String, String>> maplist = Tools.locate(filepaths.get(i), pattern);
//			System.out.println(maplist.size() + "\t" + maplist);
//			ArrayList<Map<String, String>> newlist = Tools.IsClass(maplist);
//			System.out.println(newlist.size() + "\t" + newlist);
//		}
//		
		List set = new ArrayList<String>();
		set.add("121");
		set.add("23");
		set.add("34");
//		FileUtil.appendToFile(filepath, "asdf", 23);
		//Tools.Run("D:\\BIT\\FlakyTest\\ACMdata\\codes\\12.java","line321_clone",set,"HashSet");
		
		
		
	}
}
